import { TypeWriter } from 'cheesi';
import * as React from 'react';
const base = () => {
  const message = `
      没有更多内容了^^^^^^$$$$$$$
  空空空空空空如也~
`;
  return (
    <>
      <TypeWriter message={message}></TypeWriter>
    </>
  );
};

export default base;
